import { MailOutlineRounded, PersonOutlined } from '@mui/icons-material'
import { Avatar, Box, Card, CardContent, Chip, Typography } from '@mui/material'
import useAuth from '../hooks/useAuth'
import PageHeader from '../components/common/PageHeader'
export default function Profile(){const {user}=useAuth();const initials=(user?.name||'U').split(' ').map(x=>x[0]).join('').slice(0,2).toUpperCase();return <><PageHeader eyebrow="Account" title="Your profile" description="Your workspace identity and account details."/><Card sx={{maxWidth:680}}><CardContent sx={{p:{xs:2.5,sm:4}}}><Box sx={{display:'flex',alignItems:'center',gap:2.5,mb:4}}><Avatar sx={{width:76,height:76,bgcolor:'#e5ebff',color:'#315ce7',fontSize:25,fontWeight:700}}>{initials}</Avatar><Box><Typography variant="h5" fontWeight={700}>{user?.name||'Workspace member'}</Typography><Chip label="Personal workspace" color="primary" size="small" sx={{mt:1}}/></Box></Box><Box sx={{display:'grid',gap:2}}><Box sx={{display:'flex',gap:1.5,alignItems:'center'}}><PersonOutlined color="action"/><Box><Typography fontSize={12} color="text.secondary">Full name</Typography><Typography fontWeight={600}>{user?.name||'—'}</Typography></Box></Box><Box sx={{display:'flex',gap:1.5,alignItems:'center'}}><MailOutlineRounded color="action"/><Box><Typography fontSize={12} color="text.secondary">Email address</Typography><Typography fontWeight={600}>{user?.email||'—'}</Typography></Box></Box></Box></CardContent></Card></>}
